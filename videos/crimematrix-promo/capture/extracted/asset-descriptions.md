# Asset inventory

No UI assets were captured (no-capture path).

All frames are design-led HTML/motion compositions built from CrimeMatrix brand tokens (navy `#0f172a`, gold `#f59e0b`, Plus Jakarta Sans / JetBrains Mono).

There are no screenshot filenames to assign as `asset_candidates`. Use `design-led` as the candidate marker for every visual frame.
